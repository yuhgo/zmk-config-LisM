# mark_layer キーマップ対応表

macOS JIS配列で正しく記号が入力できるように設定されています。

## 1行目（上段）

| 位置 | キーコード | macOS JIS で入力される記号 |
|------|-----------|---------------------------|
| Q | `SQT` | `'` （シングルクォート） |
| W | `DOUBLE_QUOTES` | `"` （ダブルクォート） |
| E | `EXCLAMATION` | `!` （エクスクラメーション） |
| R | `LEFT_BRACKET` | `[` （左角括弧） |
| T | `LEFT_PARENTHESIS` | `(` （左丸括弧） |
| Y | `RIGHT_PARENTHESIS` | `)` （右丸括弧） |
| U | `RIGHT_BRACKET` | `]` （右角括弧） |
| I | `PLUS` | `+` （プラス） |
| O | `AMPERSAND` | `&` （アンパサンド） |
| P | `PERCENT` | `%` （パーセント） |

## 2行目（ホーム段）

| 位置 | キーコード | macOS JIS で入力される記号 |
|------|-----------|---------------------------|
| A | `AT_SIGN` | `@` （アットマーク） |
| S | `SEMICOLON` | `;` （セミコロン） |
| D | `DOLLAR` | `$` （ドル） |
| F | `trans` | （透過：下位レイヤーの動作） |
| G | `trans` | （透過：下位レイヤーの動作） |
| H | `HASH` | `#` （ハッシュ/シャープ） |
| J | `SLASH` | `/` （スラッシュ） |
| K | `PIPE` | `\|` （パイプ） |
| L | `BACKSLASH` | `\` （バックスラッシュ） |
| RETURN位置 | `AMPERSAND` | `&` （アンパサンド） |

## 3行目（下段）

| 位置 | キーコード | macOS JIS で入力される記号 |
|------|-----------|---------------------------|
| Z | `LEFT_SHIFT` | （左シフト） |
| X | `trans` | （透過） |
| C | `COLON` | `:` （コロン） |
| V | `COMMA` | `,` （カンマ） |
| B | `MINUS` | `-` （マイナス/ハイフン） |
| N | `EQUAL` | `=` （イコール） |
| M | `PERIOD` | `.` （ピリオド） |
| , 位置 | `MINUS` | `-` （マイナス/ハイフン） |
| . 位置 | `trans` | （透過） |
| / 位置 | `trans` | （透過） |

## 4行目（最下段）

| 位置 | キーコード | macOS JIS で入力される記号 |
|------|-----------|---------------------------|
| 左1 | `trans` | （透過） |
| 左2 | `trans` | （透過） |
| 左3 | `trans` | （透過） |
| 左4 | `trans` | （透過） |
| 左5 | `trans` | （透過） |
| 左6 | `trans` | （透過） |
| 右1 | `trans` | （透過） |
| 右2 | `trans` | （透過） |
| 右3 | `trans` | （透過） |
| 右4 | `ASTERISK` | `*` （アスタリスク） |
| 右5 | `CARET` | `^` （キャレット） |
| 右6 | `GRAVE` | `` ` `` （グレイブアクセント/バッククォート） |

## 備考

- `&` (AMPERSAND) が1行目O位置と2行目RETURN位置の2箇所に重複して配置されています
- `trans` は透過設定で、下位レイヤー（default_layer）のキーがそのまま動作します
